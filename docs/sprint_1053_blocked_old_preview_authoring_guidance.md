Sprint S1053 documents blocked old-preview fixture guidance.

Blocked old-preview fixtures should include old desired-spending fields only to prove they are rejected. They should not include minimum expense fields, migration hints, or fallback mappings.

