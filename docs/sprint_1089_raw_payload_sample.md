Sprint S1089 adds a raw unwrapped payload in-memory sample.

The sample is deliberately blocked. It includes raw payload contents only inside an unwrapped payload container and does not include wrapped plan metadata.

