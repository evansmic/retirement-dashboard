Sprint S1032 adds a stable lookup helper for future test-only fixture shapes.

The lookup returns planning shapes by id and returns nothing for unknown ids. It does not infer alternate fixture names or touch the production loader.

