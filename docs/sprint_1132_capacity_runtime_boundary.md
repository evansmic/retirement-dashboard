Sprint S1132 adds capacity selector runtime boundary checks.

Selector outputs remain runtime-derived. The planning helper does not add saved output fields, engine output fields, or production import behavior.

