Sprint S1295 kept current examples deferred.

The clean save/import contract is active, but bundled example replacement remains a separate package. This avoids mixing schema wiring with broad example-data changes.

No persisted `.plan.json` fixture files are introduced.
