# S1485 Runtime Candidate Next Recommendation

Recommended next package: runtime candidate simulation.

The next package should:
- run simulations for built in-memory variants,
- keep simulation output runtime-only,
- preserve the saved plan schema,
- keep scoring and optimizer search deferred until simulation evidence is stable.

UI should remain untouched.
