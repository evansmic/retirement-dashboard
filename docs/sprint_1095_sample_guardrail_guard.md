Sprint S1095 guards sample boundary text.

Every in-memory sample must carry `mustNotDo` guardrails so sample data cannot quietly become production import wiring, saved output, or advice-like behavior.

