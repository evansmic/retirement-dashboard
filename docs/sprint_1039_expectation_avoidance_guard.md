Sprint S1039 checks that each hardening expectation has avoidance requirements.

The coverage helper requires `mustAvoid` entries so fixture hardening continues to guard against migration fallback, private-file requests, silent field loss, and partial imports.

