Sprint S1027 starts the controlled test-only fixture helper package.

The package stays in future saved-format planning. It does not wire production import behavior, change the current v2 saved schema, change engine output, or create persisted `.plan.json` files.

