Sprint S1304 identified the next broad section.

The next natural package is fresh clean-schema example implementation. Current bundled examples still feed the runtime engine shape internally, but production saved files now use clean schema.
