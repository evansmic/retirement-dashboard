# S1525 Scoring Planning Verification Scope

Verification should confirm:
- focused selector tests pass,
- full tests pass,
- build passes,
- parity probes pass,
- full probe suite passes,
- no `.plan.json` files are created.

This package changes planning selectors and docs only.
