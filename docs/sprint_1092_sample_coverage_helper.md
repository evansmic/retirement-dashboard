Sprint S1092 adds a test-only sample coverage helper.

The helper checks that each in-memory fixture sample points to a known fixture shape, matches the planned accept or block result, stays test-only, and includes guardrails.

