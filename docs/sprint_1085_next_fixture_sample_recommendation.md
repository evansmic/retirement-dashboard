Sprint S1085 records the next fixture-sample recommendation.

Recommended next step: add a raw-payload in-memory sample only if the raw-payload shape is split from the legacy old-preview shape, or pause before creating any real fixture files.

Do not create persisted `.plan.json` files, production loader branches, schema reset wiring, UI changes, or current example replacements.

