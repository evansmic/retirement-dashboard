Sprint S1277 tightened the clean reset runtime bridge.

Clean reset imports now require the minimum monthly expense floor before mapping into the current runtime plan shape. Incomplete clean-format files are blocked with calm copy instead of being converted into a zero-floor plan.

Production save format remains the current editable v2 plan file.
