Sprint S1310 added clean example runtime helpers.

Fresh clean example payloads can be wrapped as local plan files or adapted into the current runtime plan shape for existing engine and validation flows.

The engine input shape remains internal.
