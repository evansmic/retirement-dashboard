Sprint S1070 validates the in-memory sample against the accepted clean-format shape.

The test uses the existing test-only validation helper and proves required keys are present, forbidden keys are absent, and the planned import result remains `accept`.

