Sprint S1063 adds fixture verification guidance.

When actual fixtures are approved later, each batch should run focused future-format tests, full probes, and a `.plan.json` scan. Full package closeout should also run full tests, build, parity probes, full probes, and the `.plan.json` scan.

