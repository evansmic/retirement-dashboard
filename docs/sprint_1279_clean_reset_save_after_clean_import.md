Sprint S1279 pinned save-after-clean-import behavior.

A wrapped clean reset import may be opened through the validator, then saved back as the current editable schema v2 plan file. That saved file can be reopened by the current validator.

The future clean save contract remains deferred.
