Sprint S1253 pinned v2 compatibility preflight.

Expectation:
- Current v2 compatibility is an explicit decision before loader wiring.
- Current plan-file behavior remains active in this package.
- Future clean reset wiring must not silently replace current compatibility.

This protects existing local files until the switch is approved.
