Sprint S1292 updated the import policy record.

The policy now marks clean save/import as active. Current editable v2 files are no longer listed as supported at the production file boundary.

Runtime schema v2 remains an internal engine input shape.
