Sprint S1317 deferred visible example menu replacement.

The four fresh clean examples are implemented as clean-schema fixtures. The visible React example menu still uses the existing stable cards until a separate content/UI decision replaces those stories.

This avoids bundling data wiring with a user-facing menu change.
