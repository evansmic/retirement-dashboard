Sprint S1313 added clean example import smoke coverage.

Each fresh clean example can be wrapped as a local plan file, accepted by the production validator, and adapted into the current runtime plan shape.

Legacy v2 example-file support remains retired.
