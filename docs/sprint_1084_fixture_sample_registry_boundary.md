Sprint S1084 records registry boundaries.

The registry is test-only planning data. It must not become production import behavior, a saved fixture-file directory, current example data, or account optimizer input.

