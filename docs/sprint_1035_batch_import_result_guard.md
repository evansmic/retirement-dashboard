Sprint S1035 keeps planned import outcomes visible in batch validation.

The batch helper compares supplied planned outcomes to each shape's expected accept or block result. It remains test-only and does not call current import behavior.

