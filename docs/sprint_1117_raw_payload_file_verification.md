Sprint S1117 adds future raw-payload fixture file verification guidance.

If raw-payload fixture files are approved later, the batch should run focused future-format tests, full probes, and a `.plan.json` scan. Package closeout should also run full tests, build, parity probes, full probes, and the `.plan.json` scan.

