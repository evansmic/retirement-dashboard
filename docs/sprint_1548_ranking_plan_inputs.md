# Sprint 1548 - Ranking Plan Inputs

Added a planning selector for future candidate ranking inputs.

The plan records scored variant ids and score summary values so a future package can define ordering rules. The current package stops before ranking execution.

