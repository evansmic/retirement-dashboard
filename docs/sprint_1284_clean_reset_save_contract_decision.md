Sprint S1284 documented the next save contract decision.

The app can open wrapped clean reset imports, but saving still writes the current editable v2 plan file. A future package must explicitly approve changing the saved file contract.

That decision should cover examples, imports, rollback copy, and user-facing save labels together.
