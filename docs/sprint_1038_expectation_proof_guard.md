Sprint S1038 checks that each hardening expectation has proof requirements.

An expectation with no `mustProve` entries now fails the test-only coverage helper. This prevents vague fixture expectations from looking complete.

