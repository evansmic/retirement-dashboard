Sprint S1147 starts future example data planning.

This package stays planning-only. It drafts future example data and tests against the capacity selector planning model without changing current examples, saved plan schema, production imports, UI, engine output, or `.plan.json` files.

