Sprint S1080 validates the unsupported-future sample against the blocked future shape.

The test proves the future-only key is present, forbidden silent-drop keys are absent, and the planned result remains `block`.

