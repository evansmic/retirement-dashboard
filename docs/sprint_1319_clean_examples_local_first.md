Sprint S1319 confirmed local-first example handling.

Clean examples are in code fixtures and can be wrapped as local plan files in memory. No cloud account, advisor workflow, or persisted `.plan.json` fixture is introduced.
