Sprint S1082 adds a test-only in-memory fixture sample registry.

The registry groups the accepted clean-format, blocked old-preview, and blocked unsupported-future samples without creating fixture files.

