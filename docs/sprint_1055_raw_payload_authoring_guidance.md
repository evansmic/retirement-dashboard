Sprint S1055 documents raw-payload fixture guidance.

Raw-payload fixtures should prove that unwrapped JSON is not treated as a supported future plan file after the reset. They should remain test-only and should not become a renewed raw import path.

