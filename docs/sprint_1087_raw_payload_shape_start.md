Sprint S1087 starts the raw-payload fixture shape split package.

The package keeps all work test-only and in-memory. It does not create saved fixture files, persisted `.plan.json` files, production import wiring, schema reset wiring, UI changes, engine output changes, annual sequencing, or current example replacements.

