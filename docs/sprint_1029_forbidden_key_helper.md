Sprint S1029 adds a test-only forbidden-key helper for future fixture shapes.

The helper catches calculated answers and old phased-spending keys in accepted future fixtures. It reports the problem instead of deleting keys or treating the issue as a warning.

