Sprint S1062 adds a stop gate before actual fixture implementation.

Do not create fixture files until the next implementation step is explicitly approved. The current package only describes how fixture files should be authored if that step is approved later.

