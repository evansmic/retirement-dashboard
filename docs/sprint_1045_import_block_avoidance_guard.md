Sprint S1045 keeps import-block avoidance requirements explicit.

The coverage helper requires avoidance language so future fixture checks continue to block silent downgrade, raw fallback, partial imports, and migration promises.

