Sprint S1367 added runtime floor input extraction.

The selector keeps minimum monthly expenses excluding mortgage, mortgage payment, and total monthly floor explicit at runtime. It does not add saved calculated capacity or engine output fields.
